export enum Popup {
  person,
  mood,
  item_class,
  item_home,
  item_outdoor,
  closed,
}

export enum Action {
  empty,
  person,
  mood,
  item,
}

export enum Pregenerated {
  class,
  home,
  outdoor,
}
